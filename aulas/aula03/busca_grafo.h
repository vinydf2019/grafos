#ifndef BUSCA_GRAFO
#define BUSCA_GRAFO

void dfs(int u, int *p );
void dfs(int u);







/*

int visitado[MAX_VERTICES];
int pilha[MAX_VERTICES];
int topo = 0;
GrafoLista *grafo;

void ordenar_dfs(GrafoLista *grafo) {
   for (int i = 0; i = grafo->num_vertices; i++) {
       No *no = grafo->listas[i];
      int v = no->vertice;
      if (!visitado[v]) dfs(v, pilha);
}


*/